# Dépendances directes de l'application startme.
# Seules les bibliothèques réellement importées par le code sont listées ;
# pip résout automatiquement les dépendances transitives (asgiref, sqlparse,
# certifi, urllib3, etc.).
#
# Contraintes souples (>=x,<y) : on autorise les correctifs de sécurité mineurs
# tout en bloquant les montées de version majeures susceptibles de casser l'API.

Django>=5.2,<5.3        # Framework web
django-htmx>=1.27,<2    # Middleware HTMX (cf. MIDDLEWARE dans settings.py)
psutil>=7,<8            # Métriques système (widget de monitoring)
requests>=2.32,<3       # Appel HTTP pour l'IP publique (get_network_info)
python-dotenv>=1.2,<2   # Chargement du fichier .env (load_dotenv)
